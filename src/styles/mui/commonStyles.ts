import { asStyle } from "./_sx_interface";

export const commonStyles = asStyle({
  flex: {
    display: "flex",
  },
  buttonContainer: {
    display: "flex",
    justifyContent: "flex-end",
    mt: 2,
    mb: 1,
    background: "rgba(255, 255, 255, 0.05)",
    padding: 2,
    borderRadius: 2,
  },
});
