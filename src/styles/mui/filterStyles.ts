import { asStyle } from "./_sx_interface";

export const filterStyles = asStyle({
  parentFilterContainer: {
    display: "flex",
    alignItems: "center",
    gap: "1rem",
  },
  filterContainer: {
    margin: "1rem 0",
    display: "flex",
    width: "100%",
  },
});
