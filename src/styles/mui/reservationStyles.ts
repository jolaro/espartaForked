import { asStyle } from "./_sx_interface";

export const reservationStyles = asStyle({
  addButton: {
    minWidth: 50,
  },

  detailsBox: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
});
